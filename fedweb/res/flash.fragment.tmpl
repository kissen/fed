{{define "flash"}}
	{{range .Context.Flashs}}
		<div class="card">
			<div class="cardmain">
				{{.}}
			</div>
		</div>
	{{end}}

	{{range .Context.Warnings}}
		<div class="card">
			<div class="warning">
				<div class="cardmain">
					{{.}}
				</div>
			</div>
		</div>
	{{end}}

	{{range .Context.Errors}}
		<div class="card">
			<div class="error">
				<div class="cardmain">
					{{.}}
				</div>
			</div>
		</div>
	{{end}}
{{end}}
